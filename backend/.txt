

//função cadastro
//Extração do email, nome e senha no req.body(controller);

//validação do email se possui @ usando regex, validação do comprimento do nome, validação do comprimento da senha. se inválido lança o AppError("dados inválidos,400") (service.js)
//Buscar email no banco (repository.js)
//validação se email já existe no banco. se inválido lança AppError("Dados inválidos",409)(service.js)
//criptografar senha(service.js)
//Adicionar dados no banco(repository.js)
//retornar o status 201 e id, nome e email (controller.js);
// bloco catch do controller - Apenas executa next(erro) para repassar ao middleware central

//funcao login
//Extrair req.email e senha no req.body (controller)
//validar se os dados foram enviados se faltar algo lancar AppError("email e senha são obrigatorios",400) (service.js)
//busque usuario por email no banco e retorna registro ou null(repository.js)
//Se não for encontrado lançar AppError("Credenciais inválidas",401)(service.js)
//Se usuario está no banco e se a senha está correta Comparar com o que tem no banco usando bcrypt.compare(service.js)
//Se a senha estiver errada lançar um AppError("Credenciais inválidas",401);
//Gerar token jwt com o payload incluindo -  id (service.js)
//Retornar com status 200 e retornar JWT no frontend e id,nome e email (controller.js)
// bloco catch do controller - Apenas executa next(erro) para repassar ao middleware central

CREATE TABLE users  (
id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY, 
nome VARCHAR(100) NOT NULL, 
email VARCHAR(250) NOT NULL UNIQUE,
senha VARCHAR(250) NOT NULL, 
criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE  services_offered(
id INT AUTO_INCREMENT PRIMARY KEY, 
user_id INT UNSIGNED NOT NULL,
category_id INT NULL,
titulo VARCHAR(200) NOT NULL,
descricao TEXT NOT NULL,
ativo BOOLEAN DEFAULT TRUE,
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
CONSTRAINT fk_services_offered_user FOREIGN KEY(user_id) REFERENCES users(id),
CONSTRAINT fk_categories_offered_user FOREIGN KEY(category_id) REFERENCES categories(id) ON DELETE SET NULL
);

CREATE TABLE  services_wanted(
id INT AUTO_INCREMENT PRIMARY KEY, 
user_id INT UNSIGNED NOT NULL,
category_id INT,
titulo VARCHAR(200) NOT NULL,
descricao TEXT NOT NULL,
ativo BOOLEAN DEFAULT TRUE,
created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
CONSTRAINT fk_services_wanted_user FOREIGN KEY(user_id) REFERENCES users(id),
CONSTRAINT fk_categories_wanted_user FOREIGN KEY(category_id) REFERENCES categories(id) ON DELETE SET NULL
);

CREATE TABLE categories(
id INT AUTO_INCREMENT PRIMARY KEY, 
nome VARCHAR(100) NOT NULL UNIQUE
);

23:42:31	CREATE TABLE  services_offered( id INT AUTO_INCREMENT PRIMARY KEY,  user_id INT NOT NULL, category_id INT NULL, titulo VARCHAR(200) NOT NULL, descricao TEXT NOT NULL, ativo BOOLEAN DEFAULT TRUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, CONSTRAINT fk_services_offered_user FOREIGN KEY(user_id) REFERENCES users(id), CONSTRAINT fk_categories_offered_user FOREIGN KEY(category_id) REFERENCES categories(id) ON DELETE SET NULL )	Error Code: 3780. Referencing column 'user_id' and referenced column 'id' in foreign key constraint 'fk_services_offered_user' are incompatible.	0.062 sec

SERVICE
funcao criar_service(userId,titulo,descricao,categoryId)
se titulo ou descricao não vierem adicionar AppError("Campos obrigatórios ausentes"); 
se titulo.trim < 3 caracteres ou > 200, emita AppError("titulo deve ter entre três e duzentos caracteres")
se descricao.trim < 10 caracteres ,emita AppError("Descrição deve ter no mínimo 10 caracteres")

se nao tem categoryId, considere null 
se categoryId 
chama funcao do repository para buscar pelo id 
se não lança o AppError("categoria não encontrada",404); 

chamar repository.salvar(userId,titulo,descricao,categoryId)


CONTROLLER
funcao criar_controller
try
extrai do body: titulo,descricao e categoryId
extrai userId do req.usuario.id 
chama função criar do service criar(userId,titulo,descricao,categoryId)
retorna status(201) e json com o valor retornado da função criar do service

catch
chama next(erro) para o middleware central

REPOSITORY 
criar_service(userId,titulo,descricao,categoryId); 

funcao buscarCategoriaPorId(categoryId)
criar query de inserção no banco com base nos dados extraidos - SELECT COM categoryId
retorna resultado

funcao salvar(userId,titulo,descricao,categoryId)
executar insert 
retornar {id:resultado.insertId,userId,titulo,descricao,categoryId}

